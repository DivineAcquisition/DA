import { EmptyState } from '@/app/vistrial/components/ui';

export default function OnboardIndexPage() {
  return (
    <EmptyState
      title="This form is reached from a unique link"
      detail="Start onboarding from the lead’s profile in Call Intelligence after Closed Won and Commas payment."
    />
  );
}
